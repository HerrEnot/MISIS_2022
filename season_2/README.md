В домашних заданиях придерживатся следующего подхода:
1) Файлы грузятся в переменную path
2) Переменная path создается в начале файла с дз
3) При загрузке данных в датафрейм вы используете path а не ссылку на файл
